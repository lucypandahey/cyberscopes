{
  "name": "cyberscope-audits-source-mirror",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "capture:snapshots": "node scripts/capture-snapshots.mjs",
    "build:mirror": "node scripts/build-mirror.mjs",
    "dev": "python -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
